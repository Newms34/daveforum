STUFF TO ADD:
---------

FORUMS:
ability to post pictures
 - file upload better to just do with 'vanilla'angularjs? would allow connection to disable/enable btn
-DONE!

markdown help

CALENDAR:
TYPES of events (WILL need icons!): guild mish, contest, giveaway, ceremony, other

USER:
user panel + MORE extensive profile
ability to view other players profile
switch dashboard to tabs: inbox/outbox?, user chars, other users (/admin) (view users), Edit profile

gw2 api tie ins? probly not

try voip?


----NEXT
PROBLY need to fix adding post to existing thred

cron-ish job to purge old, non-confirmed users?

socket.io chat?
PROBLY not separate rooms

read/unread status on msgs (so user can see when they have new msgs!)
confirm for sent msg?
socket.io forced-logout? i.e., if authbit returns false, send msg forcing logout.
have socketio DC force a logout? make sure works on all app

WRITE ToS

SEARCH stuff

re-figure out dc-on-restart  thing to ONLY force logout for connected player